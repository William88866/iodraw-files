```mermaid
graph TD

A[输入产物<br/>项目目录 / Python 文件<br/>requirements.txt / 启动脚本]
B[触发外层质量门禁]

A --> B

subgraph D[关键工程优化]
direction LR
D1[嵌套项目识别<br/>根目录 + 一级子目录]
D2[入口回退扫描<br/>扫描子目录脚本]
end

subgraph C[三类核心门禁]
direction LR
C1[语法门禁<br/>逐文件语法检查]
C2[依赖门禁<br/>检查依赖声明文件]
C3[入口门禁<br/>检查可执行入口]
end

B --> C1
B --> C2
B --> C3

D1 -.支撑依赖门禁.-> C2
D1 -.支撑入口门禁.-> C3
D2 -.支撑入口门禁.-> C3

subgraph H[报告汇总与策略决策]
direction TBTB

subgraph E[质量报告与可观测性]
direction TB
E1[结构化质量报告]
E2[整体状态]
E3[检查项明细]
E4[失败摘要与归因]
E1 --> E2
E1 --> E3
E1 --> E4
end

subgraph F[双策略模式]
direction TB
F1{策略选择}
F2[Advisory<br/>告警但继续收口]
F3[Blocking<br/>失败即阻断]
F1 --> F2
F1 --> F3
end

end

C1 --> E1
C2 --> E1
C3 --> E1
E1 --> F1

subgraph G[交付决策与收口动作]
direction LR
G1[交付收口<br/>预览 / 导出 / 联调]
G2[阻断返回<br/>错误原因 / 修复线索 / 工程数据]
end

F2 --> G1
F3 --> G2

```